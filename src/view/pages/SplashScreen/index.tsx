import { Brand } from '../../components/Brand';

export function SplashScreen() {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-teal-9">
      <Brand color="gray-0" size="md" />
    </div>
  );
}
